<div class="container">
<h1>Welcome to my Lists</h1>
<ul>
  <% @lists.each do |list| %>
    <li><%= link_to list.name, list_path(list) %></li>
  <% end %>
</ul>
<div><%= link_to '(+ Add a new list)', new_list_path, class: 'addbtn'%></div>
</div>
